# Dependencies
node_modules/

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native projects are regenerated by CI on every build (D1).
# They are never edited by hand and never committed.
ios/
android/

# Build output
*.ipa
*.app
*.dSYM.zip

# Real data must never reach a public repository (D15).
# Demo datasets are generated, never copied from real data.
*.export.json
suivi-export-*.json
/exports/
/local-data/

# Environment
.env
.env.*

# Editors and OS
.DS_Store
*.log
npm-debug.*
yarn-debug.*
yarn-error.*
.idea/
.vscode/

# Local Claude Code worktrees
.claude/worktrees/
