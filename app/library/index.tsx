import { LibraryScreen } from '@/features/nutrition/screens/library-screen';

/**
 * Route wiring only (D10).
 *
 * BACK WHERE SECTION 3 DREW IT, at app/library/. Slice 3 had moved it into the
 * Journal's stack so that it would not cover the tab bar; requested the other
 * way (specs 14.24), and the whole reasoning now lives in _layout.tsx beside
 * it.
 */
export default LibraryScreen;
